import { FormEvent, useState } from 'react';
import { ArrowLeft, ArrowRight, CheckCircle2, LockKeyhole, Mail, UserRound } from 'lucide-react';
import { api } from '../api';
import { User } from '../types';

export default function AuthPage({ mode, onAuthenticated }: { mode: 'login' | 'register'; onAuthenticated: (user: User, token: string) => void }) {
  const register = mode === 'register';
  const [error, setError] = useState('');
  const [busy, setBusy] = useState(false);

  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); setError(''); setBusy(true);
    const values = new FormData(event.currentTarget);
    if (register && String(values.get('password1') || '') !== String(values.get('password2') || '')) {
      setError('Пароли не совпадают'); setBusy(false); return;
    }
    try {
      const payload = register
        ? { name: String(values.get('name') || values.get('username') || ''), username: String(values.get('username') || ''), email: String(values.get('email') || ''), password: String(values.get('password1') || '') }
        : { email: String(values.get('email') || ''), password: String(values.get('password') || '') };
      const result = await api<{ access_token: string; user: User }>(register ? '/auth/register' : '/auth/login', { method: 'POST', body: JSON.stringify(payload) });
      localStorage.setItem('teamflow_token', result.access_token); onAuthenticated(result.user, result.access_token);
    } catch (caught) { setError(caught instanceof Error ? caught.message : 'Не удалось выполнить запрос'); }
    finally { setBusy(false); }
  }

  return <div className="flex min-h-screen bg-zinc-950 text-zinc-100">{register && <div className="relative hidden w-1/2 items-center justify-center overflow-hidden bg-gradient-to-br from-violet-950 via-indigo-950 to-zinc-950 p-12 lg:flex"><div className="absolute left-1/4 top-1/4 h-64 w-64 rounded-full bg-violet-600/20 blur-3xl"/><div className="relative z-10 text-center"><div className="mb-8 flex items-center justify-center gap-3"><div className="flex h-12 w-12 items-center justify-center rounded-xl bg-violet-600 text-2xl font-bold">T</div><span className="text-3xl font-semibold">TeamFlow</span></div><div className="mx-auto mb-8 max-w-md rounded-3xl border border-zinc-800/50 bg-zinc-900/50 p-6 shadow-2xl"><div className="flex gap-4"><div className="h-32 flex-1 rounded-xl bg-zinc-800/50 p-3"><div className="mb-3 h-3 w-12 rounded bg-violet-500/50"/><div className="space-y-2"><div className="h-12 rounded-lg bg-zinc-800"/><div className="h-10 rounded-lg bg-zinc-800"/></div></div><div className="h-32 flex-1 rounded-xl bg-zinc-800/50 p-3"><div className="mb-3 h-3 w-16 rounded bg-blue-500/50"/><div className="h-12 rounded-lg bg-zinc-800"/></div><div className="h-32 flex-1 rounded-xl bg-zinc-800/50 p-3"><div className="mb-3 h-3 w-12 rounded bg-emerald-500/50"/><div className="h-12 rounded-lg bg-zinc-800"/></div></div></div><h2 className="mb-2 text-xl font-medium text-zinc-300">Работай эффективно.</h2><p className="text-zinc-400">Управляй задачами вместе с командой.</p><p className="mt-12 text-sm text-zinc-500">© 2026 TeamFlow — Пет-проект</p></div></div>}
    <div className="flex flex-1 items-center justify-center p-8"><div className="w-full max-w-md"><a href="/" className="mb-8 inline-flex items-center gap-2 text-zinc-400 transition-colors hover:text-zinc-100"><ArrowLeft className="h-5 w-5"/>На главную</a><div className="fade-in rounded-3xl border border-zinc-700 bg-zinc-900 p-8 shadow-2xl sm:p-12"><h1 className="mb-2 text-2xl font-bold">{register ? 'Создать аккаунт' : 'С возвращением'}</h1><p className="mb-8 text-zinc-400">{register ? 'Присоединяйся к команде 👋' : 'Войди в рабочее пространство TeamFlow'}</p>{error && <div className="mb-5 rounded-xl border border-red-500/30 bg-red-500/10 p-3 text-sm text-red-400">{error}</div>}<form onSubmit={submit} className="space-y-6">{register && <label className="block text-sm font-medium text-zinc-300">Username<div className="relative mt-2"><UserRound className="pointer-events-none absolute left-4 top-3.5 h-5 w-5 text-zinc-500"/><input name="username" required placeholder="Придумайте username" className="h-12 w-full rounded-2xl border border-zinc-800 bg-zinc-950 pl-12 pr-4 text-zinc-100 placeholder-zinc-500 focus:border-violet-500 focus:outline-none"/></div></label>}<label className="block text-sm font-medium text-zinc-300">Email<div className="relative mt-2"><Mail className="pointer-events-none absolute left-4 top-3.5 h-5 w-5 text-zinc-500"/><input type="email" name="email" required placeholder="your@email.com" className="h-12 w-full rounded-2xl border border-zinc-800 bg-zinc-950 pl-12 pr-4 text-zinc-100 placeholder-zinc-500 focus:border-violet-500 focus:outline-none"/></div></label><label className="block text-sm font-medium text-zinc-300">Пароль<div className="relative mt-2"><LockKeyhole className="pointer-events-none absolute left-4 top-3.5 h-5 w-5 text-zinc-500"/><input type="password" name={register ? 'password1' : 'password'} required minLength={6} placeholder="Введите пароль" className="h-12 w-full rounded-2xl border border-zinc-800 bg-zinc-950 pl-12 pr-4 text-zinc-100 placeholder-zinc-500 focus:border-violet-500 focus:outline-none"/></div></label>{register && <label className="block text-sm font-medium text-zinc-300">Повторите пароль<input type="password" name="password2" required minLength={6} className="mt-2 h-12 w-full rounded-2xl border border-zinc-800 bg-zinc-950 px-4 text-zinc-100 focus:border-violet-500 focus:outline-none"/></label>}{!register && <label className="flex items-center gap-2 text-sm text-zinc-400"><input type="checkbox" className="h-4 w-4 rounded bg-zinc-950 text-violet-600"/>Запомнить меня</label>}<button disabled={busy} className="flex h-12 w-full items-center justify-center gap-2 rounded-2xl bg-violet-600 font-semibold text-white transition-all hover:bg-violet-700 disabled:opacity-60">{busy ? 'Загрузка…' : register ? 'Зарегистрироваться' : 'Войти'}{register ? <CheckCircle2 className="h-5 w-5"/> : <ArrowRight className="h-5 w-5"/>}</button></form><p className="mt-8 text-center text-zinc-400">{register ? 'Уже есть аккаунт?' : 'Ещё нет аккаунта?'} <a href={register ? '/login/' : '/register/'} className="font-medium text-violet-400 hover:text-violet-500">{register ? 'Войти' : 'Создать аккаунт'}</a></p></div></div></div></div>;
}
